import React from 'react'

const Button = ({ btn }) => {
    return (
        <button className="w-full bg-[#6360AB] py-[16px] px-[64px] text-center text-white rounded">{btn}</button>
    )
}

export default Button