import React from 'react'

const Headings = ({heading}) => {
    return (
        <div>
            <h2 className="text-[36px] mb-[8px] font-inter font-semibold">{heading}</h2>
        </div>
    )
}

export default Headings